name: 上传至Alist-Terabox
on:
  repository_dispatch:
  workflow_dispatch:
    inputs:
      path:
        description: '下载路径'
        required: true
        default: 'Teraboxdriver'       
  
jobs:
  Teraboxdriver:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
     

      - name: Aria2 Install
        run: |
            sudo apt update
            sudo apt install -y youtube-dl
            #sudo pip install yt-dlp
            mkdir -p ./data
            cp -f $GITHUB_WORKSPACE/data02/* ./data
            IFS=$'\n'
            for I in $(cat ./data/not-m3u8_list.txt); do
                DOWNLOAD_FILE=$(basename $I) 
                echo $DOWNLOAD_FILE
                mkdir -p downloads
                youtube-dl -h
                youtube-dl https://861606595.tapecontent.net/radosgw/x3mV2L963vUkWp9/hOzcR46IJxeohgFzoSnXMOBT4JiwX535rRY3GmTPMCZi8re1qA4CGUAW9s9yeUXZwHeD0vL9EUQ-korhNlKw141crIE4l4_3M9SGS5mItNVl2F2HEi2ko8uO4Sx07jKPOfdlh8jVNTRQjzXVp6Sin6MF6qeYOGlPoNuXXC4zy2Hdv2iz2BS2F9RZwIGy4VsySN2bExAeYig8vGKxeLrRtrSOboMvUSkEy49dH8XmqyirxdEl7iLj0Q8XNmUskltqGiFzTuYXkg-fVbb8/FSDSS-761.mp4
            done   
            ls -al ./downloads
            ls -al


      - name: Delete workflow runs
        uses: Mattraks/delete-workflow-runs@v1.2.3
        with:
          token: ${{ secrets.PTAKEN }}
          repository: ${{ github.repository }}
          retain_days: 0
          keep_minimum_runs: 0
